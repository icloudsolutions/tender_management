<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Tree view for project.task.template -->
    <record id="project_task_template_view_tree" model="ir.ui.view">
        <field name="name">project.task.template.view.tree</field>
        <field name="model">project.task.template</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- Form view for project.task.template -->
    <record id="project_task_template_view_form" model="ir.ui.view">
        <field name="name">project.task.template.view.form</field>
        <field name="model">project.task.template</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                    <notebook>
                        <page name="tasks_page" string="Tasks">
                            <field name="task_ids">
                                <list editable="bottom">
                                    <field name="name"/>
                                    <field name="description" type="html"/>
                                    <field name="user_ids"
                                           widget="many2many_avatar_user"
                                           optional="show"
                                           domain="[('share', '=', False),
                                                    ('active', '=', True)]"/>
                                    <field name="state"/>
                                    <button name="action_open_task"
                                            type="object"
                                            title="View Task"
                                            string="View Task"
                                            class="btn btn-link pull-right"/>
                                </list>
                            </field>
                        </page>
                        <page name="stages_page" string="Stages">
                            <field name="stage_ids"/>
                        </page>
                        <page name="crm_integration_page" string="CRM Integration">
                            <group>
                                <group>
                                    <field name="auto_create_project"/>
                                    <field name="crm_stage_id" 
                                           invisible="auto_create_project == False"
                                           required="auto_create_project == True"/>
                                </group>
                            </group>
                            <div class="alert alert-info" role="alert" 
                                 invisible="auto_create_project == False">
                                <p>
                                    <strong>Auto-create Project from CRM:</strong><br/>
                                    When enabled, a project will be automatically created from this template 
                                    when a CRM opportunity reaches the selected stage.
                                </p>
                                <ul>
                                    <li>The project name will be: "[Opportunity Name] - [Template Name]"</li>
                                    <li>All tasks from this template will be created in the new project</li>
                                    <li>The project will be linked to the CRM opportunity</li>
                                    <li>Only one project per opportunity/template combination will be created</li>
                                </ul>
                            </div>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action window for project.task.template -->
    <record id="project_task_template_action" model="ir.actions.act_window">
        <field name="name">Project Templates</field>
        <field name="res_model">project.task.template</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menu item for Project Templates -->
    <menuitem id="project_task_template_menu_action"
              action="project_task_template_action"
              parent="project.menu_project_config"/>
</odoo>
