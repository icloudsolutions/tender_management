<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_smb_create_project_wizard_form" model="ir.ui.view">
            <field name="name">smb.create.project.wizard.form</field>
            <field name="model">smb.create.project.wizard</field>
            <field name="arch" type="xml">
                <form string="Create Delivery Project">
                    <group>
                        <field name="order_id" readonly="1"/>
                        <field name="name" placeholder="e.g. Delivery - SO001 - Customer Name"/>
                        <field name="partner_id" readonly="1"/>
                        <field name="user_id"/>
                        <field name="date_start"/>
                        <field name="use_task_template" widget="boolean_toggle"/>
                        <field name="task_template_id" invisible="not use_task_template"/>
                    </group>
                    <footer>
                        <button name="action_create_project" string="Create Project" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>
